services:
  bun-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: OrderLink-AdminTool
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
    env_file:
      - .env.production
    restart: unless-stopped
